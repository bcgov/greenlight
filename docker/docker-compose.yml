version: '3'
services:

  bc_registries:
    image: permitify
    environment:
      # DATABASE_URL: 'postgres://postgres:@postgres:5432/postgres'
      THE_ORG_BOOK_BASE_URL: 'https://django-devex-von-dev.pathfinder.gov.bc.ca/api/v1'
      DISCONNECTED: '${DISCONNECTED-false}'
      TEMPLATE_NAME: bc_registries
    # links:
    #   - postgres
    volumes:
      #- ./src/templates:/app/templates
      #- ./src/static:/app/static
      #- ./site_templates:/app/site_templates
      - bc_registries_wallet:/app/.indy_client/wallet
    ports:
      - 3000:8000


  worksafe_bc:
    image: permitify
    environment:
      # DATABASE_URL: 'postgres://postgres:@postgres:5432/postgres'
      THE_ORG_BOOK_BASE_URL: 'https://django-devex-von-dev.pathfinder.gov.bc.ca/api/v1'
      DISCONNECTED: '${DISCONNECTED-false}'
      TEMPLATE_NAME: worksafe_bc
    # links:
    #   - postgres
    volumes:
      # - ./src:/app
      # - ./site_templates:/app/site_templates
      - worksafe_bc_wallet:/app/.indy_client/wallet
    ports:
      - 4000:8000



  # Example new service:
  
  # liquor_board:
  #   image: permitify
  #   environment:
  #     # DATABASE_URL: 'postgres://postgres:@postgres:5432/postgres'
  #     THE_ORG_BOOK_BASE_URL: 'https://django-devex-von-dev.pathfinder.gov.bc.ca/api/v1'
  #     TEMPLATE_NAME: liquor_board
  #   # links:
  #   #   - postgres
  #   volumes:
  #     # - ./src:/app
  #     # - ./site_templates:/app/site_templates
  #     - liquor_board_wallet:/app/.indy_client/wallet
  #   ports:
  #     - 5000:8000


  # postgres:
  #   image: postgres
  #   ports:
  #     - '5432:5432'

volumes:
  worksafe_bc_wallet:
  bc_registries_wallet:
  # liquor_board_wallet:
